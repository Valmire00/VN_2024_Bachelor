{
  "extends": "stylelint-config-standard",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["extends", "apply"]
      }
    ],
    "block-no-empty": null,
    "unit-whitelist": ["em", "rem", "%", "s"]
  }
}
